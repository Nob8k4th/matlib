from setuptools import find_packages, setup


setup(
    name="matlib",
    version="0.1.0",
    description="A lightweight matrix and vector operations library",
    packages=find_packages(),
    python_requires=">=3.11",
)
